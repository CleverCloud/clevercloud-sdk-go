// Code generated by generate-models. DO NOT EDIT.

package models

// Ping The watchdog envelope carried by `POST /ping`, in both directions.  Both fields are required: the...
type Ping struct {
	Action string `json:"action"` // Echoed back as `PONG`. The request's own value is discarded.
	Data   string `json:"data"`   // Opaque payload, returned verbatim — this is what makes the probe a round-trip check rather than a...
}
