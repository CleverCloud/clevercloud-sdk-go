// Code generated by generate-services. DO NOT EDIT.

package monitoring

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Getmonitoringhealthconstraint `GET /v4/monitoring/health/{constraintName}` — one constraint, run fresh; the thin
documented wrapper over [`api_health::constraint_health`] (which carries the two
algorithm blocks — refine → 400, find → 404, else fresh run → 200/503).

Doc carrier for `getMonitoringHealthConstraint`; the framework serves the route —
do not mount.

The global-layer responses (`301` under `x-forwarded-proto: http`, an
`application/javascript` body under `?callback=`) are reachable here too and omitted
for the same reason — see [`aggregate_health_handler`].

Issue: #1722

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - constraintName: Constraint name, `^[a-z][a-z0-9-]*$` (`database`, `pulsar-heartbeat`)

# Returns the operation result or an error

Example:

	response := monitoring.Getmonitoringhealthconstraint(ctx, client, tracer, constraintName)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: monitoring
operationId: getMonitoringHealthConstraint
*/
func Getmonitoringhealthconstraint(ctx context.Context, c *client.Client, tracer trace.Tracer, constraintName string) client.Response[models.ApiHealthConstraintResult] {
	ctx, span := tracer.Start(ctx, "getMonitoringHealthConstraint", trace.WithAttributes(attribute.String("constraintName", constraintName)))
	defer span.End()

	path := utils.Path("/v4/monitoring/health/%s", constraintName)

	// Make API call
	response := client.Get[models.ApiHealthConstraintResult](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
