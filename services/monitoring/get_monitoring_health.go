// Code generated by generate-services. DO NOT EDIT.

package monitoring

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Getmonitoringhealth `GET /v4/monitoring/health` — the cached, sanitized aggregate report; the thin
documented wrapper over [`api_health::aggregate_health`] (which carries the two
algorithm blocks — cache → sanitize → 200/503).

Doc carrier for `getMonitoringHealth`; the framework serves the route — do not
mount.

**Two reachable responses the `responses(…)` block below deliberately omits.** These
routes are merged by `register_modules!` into the api-bin router *before*
`apply_global_middleware` wraps it (`crates/api/api/src/main.rs`), so they carry the
six global layers like every other product route. That makes `301 Moved Permanently`
reachable (HttpsFilter, on a request carrying `x-forwarded-proto: http`) and makes a
`?callback=` request answer `application/javascript` with `callback(<report>);`
(JsonPFilter) instead of `application/json`. Neither is a property of *this* endpoint:
both apply uniformly to every route in the merged document, and no other operation of
the ~885 documents them — listing them on these two alone would read as though the
rest were exempt. They are documented where an operator looks for them,
`crates/api/api/DEPLOY.md` §"Health probes", and the 301 is pinned by
`monitoring_health_probe_answers_through_the_global_middleware_stack` in `main.rs`.
The `responses(…)` block stays the *handler's* contract.

Issue: #1722

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := monitoring.Getmonitoringhealth(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: monitoring
operationId: getMonitoringHealth
*/
func Getmonitoringhealth(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[models.ApiHealthReport] {
	ctx, span := tracer.Start(ctx, "getMonitoringHealth")
	defer span.End()

	path := utils.Path("/v4/monitoring/health")

	// Make API call
	response := client.Get[models.ApiHealthReport](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
