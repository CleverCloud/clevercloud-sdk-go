// Code generated by generate-services. DO NOT EDIT.

package kms

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
DrainKmsCluster `POST /v4/kms/clusters/{cluster_kms_id}/drain` — stop placing on it (FR-039).

Refused while the cluster is its region's default, and the refusal names **repointing** as the
remedy rather than simply saying no: draining the default would leave the region with nowhere
to place, so the operator's next step is to designate another default first.

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - cluster_kms_id: Cluster identifier

# Returns the operation result or an error

Example:

	response := kms.DrainKmsCluster(ctx, client, tracer, cluster_kms_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: kms
operationId: drain_kms_cluster
*/
func DrainKmsCluster(ctx context.Context, c *client.Client, tracer trace.Tracer, cluster_kms_id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "drain_kms_cluster", trace.WithAttributes(attribute.String("cluster_kms_id", cluster_kms_id)))
	defer span.End()

	path := utils.Path("/v4/kms/clusters/%s/drain", cluster_kms_id)

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
