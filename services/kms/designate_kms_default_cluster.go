// Code generated by generate-services. DO NOT EDIT.

package kms

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
DesignateKmsDefaultCluster `POST /v4/kms/clusters/{cluster_kms_id}/default` — designate as its region's default (FR-041).

The region is **read from the cluster**, not taken from the caller: accepting a `region_id`
here would let a caller designate a cluster as the default of a region it does not belong to,
and the store would have to re-derive the truth anyway. The read happens after the gate.

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - cluster_kms_id: Cluster identifier

# Returns the operation result or an error

Example:

	response := kms.DesignateKmsDefaultCluster(ctx, client, tracer, cluster_kms_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: kms
operationId: designate_kms_default_cluster
*/
func DesignateKmsDefaultCluster(ctx context.Context, c *client.Client, tracer trace.Tracer, cluster_kms_id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "designate_kms_default_cluster", trace.WithAttributes(attribute.String("cluster_kms_id", cluster_kms_id)))
	defer span.End()

	path := utils.Path("/v4/kms/clusters/%s/default", cluster_kms_id)

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
