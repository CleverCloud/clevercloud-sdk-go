// Code generated by generate-services. DO NOT EDIT.

package warp10

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Warp10HealthConstraint GET /v4/warp10/health/{constraintName} — one constraint, run fresh; the thin
documented wrapper over [`api_health::constraint_health`] (which carries the two
algorithm blocks — refine → 400, find → 404, else fresh run → 200/503).

Three wire convergences landed with #2376, all deliberate:
  - an unknown name answers the OVD `clever.core.not-found` envelope
    (`{apiRequestId, code, context, error}`) instead of warp10's ad-hoc
    `{"error": …}` object — the divergence previously tracked at #2101; the `error`
    string is unchanged,
  - a name failing the `^[a-z][a-z0-9-]*$` refinement answers 400
    `clever.core.bad-request` instead of 404,
  - the sanitized `message` is present-as-null on the wire instead of omitted.

Doc carrier for `warp10_health_constraint`; the framework serves the route — do
not mount.

Source: ModuleHealthCheckComponent.scala:67-104 constraintEndpoint (Service.WARP10)
Issues: #758, #2101, #2376, #2798

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - constraintName: Constraint name, `^[a-z][a-z0-9-]*$` (`database`)

# Returns the operation result or an error

Example:

	response := warp10.Warp10HealthConstraint(ctx, client, tracer, constraintName)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: warp10
operationId: warp10_health_constraint
*/
func Warp10HealthConstraint(ctx context.Context, c *client.Client, tracer trace.Tracer, constraintName string) client.Response[models.ApiHealthConstraintResult] {
	ctx, span := tracer.Start(ctx, "warp10_health_constraint", trace.WithAttributes(attribute.String("constraintName", constraintName)))
	defer span.End()

	path := utils.Path("/v4/warp10/health/%s", constraintName)

	// Make API call
	response := client.Get[models.ApiHealthConstraintResult](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
