// Code generated by generate-services. DO NOT EDIT.

package self

import (
	"fmt"
	"net/url"
	"strings"
)

// Option defines a functional option for self operations
type Option func(*Options)

// Options holds query parameters for self operations
type Options struct {
	Validationkey *string `url:"validationKey,omitempty"`
}

// WithValidationkey sets the validationKey query parameter
func WithValidationkey(validationKey string) Option {
	return func(o *Options) {
		o.Validationkey = &validationKey
	}
}

// buildQueryString builds a query string from options
func buildQueryString(opts ...Option) string {
	options := &Options{}
	for _, opt := range opts {
		opt(options)
	}

	var params []string
	if options.Validationkey != nil {
		params = append(params, fmt.Sprintf("validationKey=%s", url.QueryEscape(*options.Validationkey)))
	}

	if len(params) == 0 {
		return ""
	}
	return strings.Join(params, "&")
}
