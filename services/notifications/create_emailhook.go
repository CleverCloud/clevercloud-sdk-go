// Code generated by generate-services. DO NOT EDIT.

package notifications

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
CreateEmailhook POST /v2/notifications/emailhooks/{owner_id} — create an email hook.

Source: notification-api POST /emailhooks/{ownerId}
Issue: #262
Algorithm: Verify notification access, rate-limit, validate name non-empty and at least one notified target, convert events/notified to the typed Mongo model (unknown keys → 422), reject a duplicate (owner, name) with 409 Conflict (fixes #3232), insert an Emailhook document (bare UUID id, createdAt now) into the `emailhooks` collection, return 201 Created with EmailHookView.
Legacy: notification-api Emailhook.create mints a bare UUID and EmailhookController returns 201 Created.
Conformity: faithful (status 201, refs #1053; MongoDB backend #1054) except the 409 duplicate guard — a deliberate deviation (legacy's bare insert is what duplicated every user's default hook, #3232) matching the PG store's UNIQUE (owner_id, name) (#3210).

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - owner_id: Owner ID
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := notifications.CreateEmailhook(ctx, client, tracer, owner_id, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: notifications
operationId: create_emailhook
*/
func CreateEmailhook(ctx context.Context, c *client.Client, tracer trace.Tracer, owner_id string, requestBody *models.WannabeEmailHook) client.Response[models.EmailHookView] {
	ctx, span := tracer.Start(ctx, "create_emailhook", trace.WithAttributes(attribute.String("owner_id", owner_id)))
	defer span.End()

	path := utils.Path("/v2/notifications/emailhooks/%s", owner_id)

	// Make API call
	response := client.Post[models.EmailHookView](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
