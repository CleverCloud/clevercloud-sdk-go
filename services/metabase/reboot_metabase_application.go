// Code generated by generate-services. DO NOT EDIT.

package metabase

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Rebootmetabaseapplication POST .../addons/{id}/reboot — restart the Java app **with** build cache.

Source: references/legacy/ovd/modules/metabase/services/MetabaseProviderService.scala — rebootAddon(rebuild=false)
Behavior: resolve the addon and authorize the caller against its owner

	(404 when it is absent or soft-deleted, 403 for a non-member), require a
	Java application on the row (a NULL column logs a WARN and 404s), then ask
	cc-api to restart it **with** the build cache. Answers 204 once the restart
	is accepted; a cc-api failure is a 500. With no outbound clients
	(airgap/test) nothing is restarted and the 204 still stands.

Issue: #8

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - addonMetabaseId: Metabase addon ID

# Returns the operation result or an error

Example:

	response := metabase.Rebootmetabaseapplication(ctx, client, tracer, addonMetabaseId)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: metabase
operationId: rebootMetabaseApplication
*/
func Rebootmetabaseapplication(ctx context.Context, c *client.Client, tracer trace.Tracer, addonMetabaseId string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "rebootMetabaseApplication", trace.WithAttributes(attribute.String("addonMetabaseId", addonMetabaseId)))
	defer span.End()

	path := utils.Path("/v4/addon-providers/addon-metabase/addons/%s/reboot", addonMetabaseId)

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
