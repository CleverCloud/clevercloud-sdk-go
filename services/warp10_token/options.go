// Code generated by generate-services. DO NOT EDIT.

package warp10_token

import (
	"fmt"
	"net/url"
	"strings"
)

// Option defines a functional option for warp10_token operations
type Option func(*Options)

// Options holds query parameters for warp10_token operations
type Options struct {
	Limit *int    `url:"limit,omitempty"`
	Scope *string `url:"scope,omitempty"`
	Since *string `url:"since,omitempty"`
}

// WithLimit sets the limit query parameter
func WithLimit(limit int) Option {
	return func(o *Options) {
		o.Limit = &limit
	}
}

// WithScope sets the scope query parameter
func WithScope(scope string) Option {
	return func(o *Options) {
		o.Scope = &scope
	}
}

// WithSince sets the since query parameter
func WithSince(since string) Option {
	return func(o *Options) {
		o.Since = &since
	}
}

// buildQueryString builds a query string from options
func buildQueryString(opts ...Option) string {
	options := &Options{}
	for _, opt := range opts {
		opt(options)
	}

	var params []string
	if options.Limit != nil {
		params = append(params, fmt.Sprintf("limit=%d", *options.Limit))
	}
	if options.Scope != nil {
		params = append(params, fmt.Sprintf("scope=%s", url.QueryEscape(*options.Scope)))
	}
	if options.Since != nil {
		params = append(params, fmt.Sprintf("since=%s", url.QueryEscape(*options.Since)))
	}

	if len(params) == 0 {
		return ""
	}
	return strings.Join(params, "&")
}
