// Code generated by generate-services. DO NOT EDIT.

package infrastructure

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
ListDeployments GET /v4/infrastructure/deployments

Source: axo-only — OVD's compute API has NO list-deployments route (its
deployment endpoints are exactly create/get/deleteVM, `api/routes.scala`
endpoint list; its archive was write-only). This route is backed by the
scheduler archive's `list_archived_tasks` (`ArchivalStrategy::Index`), a
documented axo deviation — `api-task-scheduler` `command.rs`.
Behavior: list deployments from the ZK archive, optionally filtered by status.
Issue: #664, #2246

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := infrastructure.ListDeployments(ctx, client, tracer, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: infrastructure
operationId: list_deployments
*/
func ListDeployments(ctx context.Context, c *client.Client, tracer trace.Tracer, opts ...Option) client.Response[[]models.DeploymentOutput] {
	ctx, span := tracer.Start(ctx, "list_deployments")
	defer span.End()

	path := utils.Path("/v4/infrastructure/deployments")

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[[]models.DeploymentOutput](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
