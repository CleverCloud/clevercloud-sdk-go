// Code generated by generate-services. DO NOT EDIT.

package products

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Getzones GET /v2/products/zones — list active zones. **Public**: auth widens, never gates.

Source: cc-api ProductsAPI.java:194-195 — `ah.auth(crc)` is passed *into*
`aph.getAllowedZones(tk)` and never tested, so a missing, malformed or expired
credential yields an empty owner list rather than a rejection
(`AddonProviderHelper.java:136-145`), and `Zone.isAllowedFor` admits every zone
whose `allowList` is empty (`Zone.java:60-70`). Confirmed against production:
no header, a garbage `OAuth` header and a garbage `Bearer` all return 200 with
the full public list.

Authenticated, the caller's organisations widen the public set through
`zone_access` (cc-api `Zone.java:37-39,60-70`); unauthenticated returns only the
zones with no `zone_access` row, since a zone granted to one owner must not leak
to a public caller. The previous partner-based filter is not what cc-api applies
here — see `ZoneRepository::find_visible_to_user`.
Issues: #102, #1091, #2361, #2424

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := products.Getzones(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: products
operationId: getZones
*/
func Getzones(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[[]models.ZoneView] {
	ctx, span := tracer.Start(ctx, "getZones")
	defer span.End()

	path := utils.Path("/v2/products/zones")

	// Make API call
	response := client.Get[[]models.ZoneView](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
