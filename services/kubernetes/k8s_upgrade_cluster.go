// Code generated by generate-services. DO NOT EDIT.

package kubernetes

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
K8sUpgradeCluster **Legacy**: ovd ClusterController.scala:183 updateVersion()
**Algorithm**:
  - Updates kubernetes_version, sets status=UPDATING in TX
  - Inserts cluster_status record; actual VM redeployment is async

**Conformity**: YES

POST /v4/kubernetes/organisations/{owner_id}/clusters/{cluster_id}/upgrade — upgrade cluster K8s version.

Source: references/legacy/ovd/modules/kubernetes/controllers/ClusterController.scala — updateVersion
Source: references/legacy/ovd/modules/kubernetes/repositories/ClusterRepository.scala — queries.updateKubernetesVersion + updateStatus
Behavior: updates kubernetes_version and sets status to UPDATING in a transaction.

	Also inserts a cluster_status record. Actual upgrade requires VM redeployment.
	The transaction also records the outgoing version on the TO_UPGRADE
	status row (`labels -> 'upgradeFromVersion'`, OVD's own transition-label
	pattern) so the redeploy's plugin reconcile can diff `from` -> `to` (#2356).

Issue: #8, #2356, #2865

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - owner_id: Owner (org) ID
  - cluster_id: Cluster ID
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := kubernetes.K8sUpgradeCluster(ctx, client, tracer, owner_id, cluster_id, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: kubernetes
operationId: k8s_upgrade_cluster
*/
func K8sUpgradeCluster(ctx context.Context, c *client.Client, tracer trace.Tracer, owner_id string, cluster_id string, requestBody *models.PatchClusterVersion) client.Response[models.ClusterView] {
	ctx, span := tracer.Start(ctx, "k8s_upgrade_cluster", trace.WithAttributes(attribute.String("owner_id", owner_id), attribute.String("cluster_id", cluster_id)))
	defer span.End()

	path := utils.Path("/v4/kubernetes/organisations/%s/clusters/%s/upgrade", owner_id, cluster_id)

	// Make API call
	response := client.Post[models.ClusterView](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
