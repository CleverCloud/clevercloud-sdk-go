// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlMigrate `POST /resources/{addonId}/migrations`, `Application.migrate` —
`MigrationDriver.migrate`'s synchronous prelude: validate the body, look
the addon up, insert the preparation row, transition `RetrieveAddon`,
harvest-or-skip the source oids, then check exactly one migration is
RUNNING for this addon. The kind-specific arm (`run_migration_arm`) is
spawned UNAWAITED — the HTTP answer is the IN-MEMORY preparation this
prelude built (`steps: []`, `status: RUNNING`), NEVER the DB state the
prelude's own step writes already produced (map trap T1).

Body deviation (D-015 class, same as [`provision`]'s doc comment): an
axum `JsonRejection` renders its own 400/415 body instead of Play's error
page.

Source: rewrite/rust/src/migration/{routes,controller,driver}.rs — migrate
Issue: #712 (migration slice)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - addon_id: PostgreSQL addon string id (postgresql_<uuid>)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlMigrate(ctx, client, tracer, addon_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_migrate
*/
func PostgresqlMigrate(ctx context.Context, c *client.Client, tracer trace.Tracer, addon_id string) client.Response[models.PostgresqlMigrationConsoleView] {
	ctx, span := tracer.Start(ctx, "postgresql_migrate", trace.WithAttributes(attribute.String("addon_id", addon_id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/resources/%s/migrations", addon_id)

	// Make API call
	response := client.Post[models.PostgresqlMigrationConsoleView](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
