// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlGetConfiguration `GET /internal/{id}/configuration` — the postgresql.conf text an instance
pulls at boot (per-addon `Basic <stringId>:<password>` auth — see
`router`/`bare_router`'s `password_authed` sub-router). A running
(RUNNING or RECOVERING) migration swaps in the target plan/version before
the computation; otherwise the addon's OWN current plan/version is used.
A shared (or unparsable-target) plan is a 400 with the reference's
`ProvisionResponse` envelope.

#1416-class IDOR guard: the Basic-authenticated addon (injected by
`require_addon_password_auth` as [`AuthenticatedAddon`]) must match the
path `{id}` — a foreign addon's own valid password must never read this
addon's configuration text (same guard `crates/addons/redis` and
`crates/addons/mysql` apply to their own per-addon config/passphrase
routes).

Source: rewrite/rust/src/configuration/controller.rs — get_configuration

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: PostgreSQL addon string id (postgresql_<uuid>)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlGetConfiguration(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_get_configuration
*/
func PostgresqlGetConfiguration(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_get_configuration", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/%s/configuration", id)

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
