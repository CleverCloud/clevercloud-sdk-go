// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlInternalGetVersion `GET /internal/versions/{version}` — a single version's availability
window.

An unknown label is a 404 with the list of known labels as a hint; a DB
failure past that point is a 500 carrying the driver's message (unlike the
listing endpoints above — the controller builds this response body by
matching, never from the version string, so it never becomes 404 by
surprise).

Source: rewrite/rust/src/versions/controller.rs — get_version, error_response

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - version: PostgreSQL major version label, e.g. "17"

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlInternalGetVersion(ctx, client, tracer, version)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_internal_get_version
*/
func PostgresqlInternalGetVersion(ctx context.Context, c *client.Client, tracer trace.Tracer, version string) client.Response[models.PostgresqlVersion] {
	ctx, span := tracer.Start(ctx, "postgresql_internal_get_version", trace.WithAttributes(attribute.String("version", version)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/versions/%s", version)

	// Make API call
	response := client.Get[models.PostgresqlVersion](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
