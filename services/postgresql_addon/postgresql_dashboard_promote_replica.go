// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlDashboardPromoteReplica `POST /dashboard/promoteReplica` — `pg_promote()` against the dedicated
addon's own root credentials, then flips `config_role` to `master` and
fires a best-effort PagerDuty alert on the outcome. An addon already
primary (`config_role == "master"`) is refused before any dataplane work
— `executePromotion`'s own not-a-replica gate.

Source: rewrite/rust/src/dashboard/{routes,dataplane,writes,pagerduty}.rs — promote_replica, executePromotion
Issue: #712 (dashboard slice, part 2)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlDashboardPromoteReplica(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_dashboard_promote_replica
*/
func PostgresqlDashboardPromoteReplica(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[models.PostgresqlProvisionResponse] {
	ctx, span := tracer.Start(ctx, "postgresql_dashboard_promote_replica")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/dashboard/promoteReplica")

	// Make API call
	response := client.Post[models.PostgresqlProvisionResponse](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
