// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlBackupRegister `PUT /internal/{id}/registerBackup` — turn on backups for a dedicated
addon. Gates in reference order: unknown addon 404, backups already on
400, shared plan 400, then the slot/owner registration and the do-backup
flag write. Owner-vs-slot store: the entity registers under the Owner
store (with the addon's zone attached) when that zone is listed in
`DEDICATED_REGIONS_WITH_OWNER_BACKUP`, plain Slot otherwise —
`owner_id` is set on BOTH branches, only `zone` is Owner-only (dropping it
on the Owner branch 404s the owner-scoped listing, issue #1408-class).

Source: rewrite/rust/src/backup_urls/controller.rs — register_addon_backup

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: PostgreSQL addon string id (postgresql_<uuid>)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlBackupRegister(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_backup_register
*/
func PostgresqlBackupRegister(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[models.AddonNotFoundView] {
	ctx, span := tracer.Start(ctx, "postgresql_backup_register", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/%s/registerBackup", id)

	// Make API call
	response := client.Put[models.AddonNotFoundView](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
