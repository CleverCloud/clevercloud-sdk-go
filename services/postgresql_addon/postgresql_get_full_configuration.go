// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlGetFullConfiguration `GET /internal/{id}/configuration/full` — the layered configuration as
JSON (internal auth): the plan's untouched default, the addon's raw
custom override, and the merged/served result. An unknown addon OR a
shared plan renders the SAME 404 envelope — the reference for-comprehension
collapses both `None`s, including its `"doesn't not exist"` typo, kept
byte-exact.

Source: rewrite/rust/src/configuration/controller.rs — get_full_configuration

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: PostgreSQL addon string id (postgresql_<uuid>)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlGetFullConfiguration(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_get_full_configuration
*/
func PostgresqlGetFullConfiguration(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[models.PostgresqlFullConfigurationView] {
	ctx, span := tracer.Start(ctx, "postgresql_get_full_configuration", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/%s/configuration/full", id)

	// Make API call
	response := client.Get[models.PostgresqlFullConfigurationView](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
