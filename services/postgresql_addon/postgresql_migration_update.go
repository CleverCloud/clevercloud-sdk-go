// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlMigrationUpdate `PUT /internal/migrations/{migrationId}`, `Internal.migrationUpdate`: the
path id appears ONLY in the "Can't find Migration" error, the body id
drives everything (map trap T4). `$` is stripped from the step message.
The migration must exist as a RUNTIME row (400 otherwise) and its OWN
addon must still exist (404 `Can't find Addon: {migration.addonId}.`,
the addon id being the MIGRATION's, not a path parameter) before the
manager runs; a synchronous throw inside the manager (DUMP_RESTORE_RUNNING
after FAILED/RECOVERED with no `instanceId`) is Play's bare 500.

Source: references/legacy/addon-api/postgresql/app/controllers/Internal.scala:322-337 — migrationUpdate
Source: rewrite/rust/src/migration/controller.rs — migration_update
Issue: #712 (migration slice)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - migration_id: The migration's uuid (used only in the 'Can't find Migration' error text — the BODY's own migrationId drives the transition)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlMigrationUpdate(ctx, client, tracer, migration_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_migration_update
*/
func PostgresqlMigrationUpdate(ctx context.Context, c *client.Client, tracer trace.Tracer, migration_id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_migration_update", trace.WithAttributes(attribute.String("migration_id", migration_id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/migrations/%s", migration_id)

	// Make API call
	response := client.Put[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
