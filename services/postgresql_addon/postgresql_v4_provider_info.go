// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlV4ProviderInfo `GET /v4/addon-providers/{providerId}` — the V1 shape. `target` selects
which version list is advertised: `migration` narrows it to what an
existing addon can move to, anything else (including an absent parameter)
lists what a new addon can be ordered with. `providerId` is accepted and
ignored — this API only ever serves PostgreSQL.

Source: rewrite/rust/src/provider/controller.rs — provider_infos

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - provider_id: Provider id (accepted, ignored — this API only ever serves PostgreSQL)
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlV4ProviderInfo(ctx, client, tracer, provider_id, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_v4_provider_info
*/
func PostgresqlV4ProviderInfo(ctx context.Context, c *client.Client, tracer trace.Tracer, provider_id string, opts ...Option) client.Response[models.PostgresqlProvidersConsoleView] {
	ctx, span := tracer.Start(ctx, "postgresql_v4_provider_info", trace.WithAttributes(attribute.String("provider_id", provider_id)))
	defer span.End()

	path := utils.Path("/v4/addon-providers/%s", provider_id)

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[models.PostgresqlProvidersConsoleView](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
