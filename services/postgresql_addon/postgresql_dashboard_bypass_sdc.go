// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlDashboardBypassSdc `POST /dashboard/bypassSDC` — discover the VM's direct host over the
supernova info RPC (`getSDCConfiguration`: hostname, appPort, tcpPort),
`withDiscoveredDirectHost` + `updateDirectHV` (`upsertDirectHostCoordinates`:
the `provision_directhost_config` row plus the legacy `config_hv_*`
double-write, `config_direct_host_only = proxyMode == DirectHost`), then
— FerretDB on AND a `tcpPort` discovered — `updateFerretDB` with the
direct port, then push the (now `_DIRECT_*`-carrying) `toMap` to the
vendor, AWAITED — unlike `genpwd`'s fire-and-forget push, a vendor
failure here IS the visible outcome. Shared plans are refused before any
RPC; missing cc-api vendor credentials answer the crate's own Cloud
Versatile 501 (this action's entire point is the awaited vendor push, so
— unlike `genpwd`'s optional, skippable one — an absent credential pair
cannot be silently downgraded to a no-op success here).

A Private LoadBalancer addon (`withDiscoveredDirectHost` returns `this`,
no `hvHostname`/`hvPort` to write) is `updateDirectHV`'s own Left,
`Missing hvHostname or hvPort in addon config` — the JSON 500.

Source: references/legacy/addon-api/postgresql/app/controllers/Dashboard.scala:238-283 — bypassSDC
Source: references/legacy/addon-api/postgresql/app/models/addon_module.scala:1221-1229 — updateDirectHV
Source: rewrite/rust/src/dashboard/{routes,supernova,writes}.rs — bypass_sdc
Source: crates/addons/mysql/src/controller.rs — dashboard_bypass_sdc
Issue: #712 (dashboard slice, part 2)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlDashboardBypassSdc(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_dashboard_bypass_sdc
*/
func PostgresqlDashboardBypassSdc(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[models.PostgresqlProvisionResponse] {
	ctx, span := tracer.Start(ctx, "postgresql_dashboard_bypass_sdc")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/dashboard/bypassSDC")

	// Make API call
	response := client.Post[models.PostgresqlProvisionResponse](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
