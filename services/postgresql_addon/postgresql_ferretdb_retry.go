// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlFerretdbRetry `POST /internal/ferretdb/{addonId}/retry`, `Internal.retryFerretDBProvisioning`.
Only FAILED or PAUSED pass the guard (map trap T13); success fires the
configure actor with attempt 1 BEFORE answering, `#[cfg(feature = "messaging")]`
only — a `messaging`-less build (or an unconfigured publisher) still resets
the row but never drives it further, matching the reference's `Option<&Actor>`
shape.

Source: rewrite/rust/src/ferretdb/controller.rs — retry_ferretdb_provisioning

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - addon_id: PostgreSQL addon string id (postgresql_<uuid>)
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlFerretdbRetry(ctx, client, tracer, addon_id, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_ferretdb_retry
*/
func PostgresqlFerretdbRetry(ctx context.Context, c *client.Client, tracer trace.Tracer, addon_id string, opts ...Option) client.Response[models.PostgresqlFerretDbRetryResponseView] {
	ctx, span := tracer.Start(ctx, "postgresql_ferretdb_retry", trace.WithAttributes(attribute.String("addon_id", addon_id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/ferretdb/%s/retry", addon_id)

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Post[models.PostgresqlFerretDbRetryResponseView](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
