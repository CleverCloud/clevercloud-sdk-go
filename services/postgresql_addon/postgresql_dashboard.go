// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlDashboard `GET /dashboard` — the admin HTML page. Content is stamped `no-cache,
no-store, must-revalidate` (the reference sends the same three headers on
every render): the page carries live credentials, must never be cached by
an intermediate.

Source: rewrite/rust/src/dashboard/{routes,views}.rs — dashboard, DashboardPage::build
Issue: #712 (dashboard slice, part 1)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlDashboard(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_dashboard
*/
func PostgresqlDashboard(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_dashboard")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/dashboard")

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
