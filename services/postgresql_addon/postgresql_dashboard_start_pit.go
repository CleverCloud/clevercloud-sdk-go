// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlDashboardStartPit `POST /dashboard/backups` (startPIT) — fire a Point-in-Time snapshot
order, then answer 200 with the config map REGARDLESS of the publish
outcome (the reference discards the `Either` of both sends). Two paths,
selected on the CLUSTER row (not the plan): a cluster carrying BOTH
`backup_routing_key` and `backup_instance_id` gets an action-dispatcher
`InstanceOrder` on `clever.orders`/`<backup_routing_key>`; anything else
(no cluster — every dedicated addon — or a cluster missing either column)
gets the legacy `{"action":"take_snapshot",…}` on
`<ADDON_NAME>-addon.instr`/`instr.take_snapshot` with the `target`
header from `start_pit_target`. The one failure surfaced is that
header's `cluster.get` crash — a shared addon whose cluster row is gone —
a bare 500.

Both publishes are AWAITED (the reference's `sendMessageAsJson` is
synchronous on the channel) then discarded; an unconfigured publisher
(no `messaging` feature / no broker) is the same discarded outcome — the
200 still goes out, exactly like a broker the reference cannot reach.

Source: references/legacy/addon-api/postgresql/app/controllers/Dashboard.scala:129-164 — startPIT
Source: references/legacy/addonprovider-lib/src/main/scala/com/clevercloud/addonproviderlib/amqp/AmqpClient.scala:218-220 — sendActionDispatcherMessage
Source: rewrite/rust/src/dashboard/{routes,actions}.rs — start_pit
Issue: #712 (dashboard slice, part 2)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlDashboardStartPit(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_dashboard_start_pit
*/
func PostgresqlDashboardStartPit(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[models.PostgresqlProvisionResponse] {
	ctx, span := tracer.Start(ctx, "postgresql_dashboard_start_pit")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/dashboard/backups")

	// Make API call
	response := client.Post[models.PostgresqlProvisionResponse](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
