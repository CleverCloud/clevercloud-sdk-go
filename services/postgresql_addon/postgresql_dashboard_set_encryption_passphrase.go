// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlDashboardSetEncryptionPassphrase `POST /dashboard/setEncryptionPassphrase` — mints a 30-lowercase-alphanumeric
passphrase, seals it under `ENCRYPTION_KEY` and stores cipher+nonce on the
row. Shares its generation/storage shape with the broker's
[`encrypt_addon`] (same "A key already exists…" gate, same
`random_alphanumeric(30).to_lowercase()` draw, same success wording) —
factored into `generate_and_store_passphrase` so the two call sites
cannot drift.

Source: rewrite/rust/src/dashboard/{routes,passphrase}.rs — set_encryption_passphrase
Issue: #712 (dashboard slice, part 1)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlDashboardSetEncryptionPassphrase(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_dashboard_set_encryption_passphrase
*/
func PostgresqlDashboardSetEncryptionPassphrase(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[models.PostgresqlProvisionResponse] {
	ctx, span := tracer.Start(ctx, "postgresql_dashboard_set_encryption_passphrase")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/dashboard/setEncryptionPassphrase")

	// Make API call
	response := client.Post[models.PostgresqlProvisionResponse](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
