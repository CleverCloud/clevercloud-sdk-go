// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlDashboardGeneratePassword `POST /dashboard/genpwd` — rotate the application password. Shared plans
rotate on the cluster; dedicated on the VM (root credentials). The vendor
PUT (`VendorAPIClient.updateAddon`: `vendor_update_addon_body` — the
cluster-aware map PLUS `POSTGRESQL_ADDON_MAX_CONNECTIONS`) is
fire-and-forget (T7 in the reference's own trap numbering: the 200 races
the env-var update), the control-plane write is synchronous — its
failure is a bare 500.

Missing cc-api vendor credentials answer the crate's Cloud Versatile 501
BEFORE any dataplane write (the reference's `configuration.get` throw
inside `updateAddon` would fire AFTER the ALTER USER, leaving the
dataplane rotated and the row stale — the same credential pair
[`dashboard_bypass_sdc`] refuses up front, so the two never diverge on
posture).

Source: references/legacy/addon-api/postgresql/app/controllers/Dashboard.scala:170-181 — generatePassword
Source: references/legacy/addon-api/postgresql/app/util/VendorAPIClient.scala:19-24 — updateAddon
Source: rewrite/rust/src/dashboard/{routes,dataplane,actions}.rs — generate_password
Issue: #712 (dashboard slice, part 1)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlDashboardGeneratePassword(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_dashboard_generate_password
*/
func PostgresqlDashboardGeneratePassword(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[models.PostgresqlProvisionResponse] {
	ctx, span := tracer.Start(ctx, "postgresql_dashboard_generate_password")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/dashboard/genpwd")

	// Make API call
	response := client.Post[models.PostgresqlProvisionResponse](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
