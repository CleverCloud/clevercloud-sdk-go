// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlDeprovision `DELETE /resources/{addonId}` — soft-delete: drop the cluster user
(shared) or publish the `del_user`/`REMOVE_ADDON` instructions
(dedicated), mark `TO_DELETE`, unregister the addon's `provision_database`
row(s). Physical DB/VM removal is deferred to the cleanup worker, as in
the oracle.

An unknown addon is a 400 here (unlike [`encrypt_addon`]'s bare 404); the
re-read after marking `TO_DELETE` and its 500-if-suddenly-gone are the
oracle's own odd trap, reproduced verbatim.

Source: rewrite/rust/src/provisioning/{routes,controller,driver}.rs — deprovision

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - addon_id: PostgreSQL addon string id (postgresql_<uuid>)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlDeprovision(ctx, client, tracer, addon_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_deprovision
*/
func PostgresqlDeprovision(ctx context.Context, c *client.Client, tracer trace.Tracer, addon_id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_deprovision", trace.WithAttributes(attribute.String("addon_id", addon_id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/resources/%s", addon_id)

	// Make API call
	response := client.Delete[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
