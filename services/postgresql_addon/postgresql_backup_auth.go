// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlBackupAuth `GET /internal/{dbname}/backupauth` — `stringId token`, space-separated,
for the VM's clever-backup upload auth.

Deviation D-015: an unknown dbname is a bare 500 with NO body — the
reference calls `.get` on the lookup, which throws for a missing addon,
and Play renders its (unreproduced) default 500 page.

Source: rewrite/rust/src/backup_directory/controller.rs — backup_auth

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: Database name (NOT an addon id — see the router's naming comment)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlBackupAuth(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_backup_auth
*/
func PostgresqlBackupAuth(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_backup_auth", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/%s/backupauth", id)

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
