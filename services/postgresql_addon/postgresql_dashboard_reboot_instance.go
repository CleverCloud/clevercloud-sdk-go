// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlDashboardRebootInstance `POST /dashboard/rebootInstance` — an info RPC for the addon's running
instances, then a fire-and-forget hard reboot per instance. Always 200
once the RPC resolves; the RPC itself failing is the reference's failed
Future — a BARE 500 (D-015 class), messaging not configured the crate's
own envelope 500 — never a false "in progress" (matches
`crates/addons/mysql`'s own #518097 fix: a caller must never be told a
reboot is under way when nothing could actually be dispatched).

Source: references/legacy/addon-api/postgresql/app/controllers/Dashboard.scala:191-197 — rebootInstance
Source: rewrite/rust/src/dashboard/{routes,supernova}.rs — reboot_instance, running_instances, reboot_instance
Source: crates/addons/mysql/src/controller.rs — reboot_instance
Issue: #712 (dashboard slice, part 2)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlDashboardRebootInstance(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_dashboard_reboot_instance
*/
func PostgresqlDashboardRebootInstance(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_dashboard_reboot_instance")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/dashboard/rebootInstance")

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
