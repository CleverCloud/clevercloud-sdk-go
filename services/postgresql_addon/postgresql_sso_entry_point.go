// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlSsoEntryPoint `POST /sso` — the console single-sign-on handshake. Validates the SSO
signature (`core_addon_provider::validate_sso` — a plain SHA-512 hex
digest over `id:user_id:email:navData:salt:timestamp`, NOT a keyed HMAC
despite the field's legacy name), mints a Sessioned token
(`core_addon_provider::crypto::create_session_token`, secretbox-sealed
under `ENCRYPTION_KEY`) and serves a 200 HTML page that meta-refreshes to
`dashboard?token=…` (relative, so it resolves under both `router` and
`bare_router`). A missing SSO salt or encryption key answers 501 (Cloud
Versatile); an unknown addon answers 400.

Source: rewrite/rust/src/dashboard/{sso,session,routes}.rs — sso_entry_point
Issue: #712 (dashboard slice, part 1)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlSsoEntryPoint(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_sso_entry_point
*/
func PostgresqlSsoEntryPoint(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_sso_entry_point")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/sso")

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
