// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlDeployInstruction `GET /internal/{id}/instruction`, `Internal.getDeployInstruction`. Three
gates collapsing into one 404 (`dedicated_addon_not_found`): unknown
addon, shared plan, missing `provision_database` row. Past the gates: a
`deployment` row insert (BEFORE the vendor call, regardless of its
outcome), the vendor `logscollector` GET, then the `BdsInstruction` JSON.

An addon whose stored locale doesn't parse (`provision_database.locale`)
crashes here — a bare 500, crash parity with the reference's
`DatabaseLocale.getDatabaseLocale(...).toOption.get`. This crate's
`provision.owner_id` is `NOT NULL` (unlike the reference's own
`Option<String>` modeling of it), so the reference's separate
missing-owner crash gate is unreachable here by construction — no
defensive branch needed.

Axo addition: unconfigured CC API vendor credentials answer 501 — see
[`update_configuration_on_ccapi`]'s doc comment.

Source: rewrite/rust/src/deploy/controller.rs — get_deploy_instruction
Issue: #712 (deploy slice)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: PostgreSQL addon string id (postgresql_<uuid>)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlDeployInstruction(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_deploy_instruction
*/
func PostgresqlDeployInstruction(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[models.PostgresqlBdsInstruction] {
	ctx, span := tracer.Start(ctx, "postgresql_deploy_instruction", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/%s/instruction", id)

	// Make API call
	response := client.Get[models.PostgresqlBdsInstruction](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
