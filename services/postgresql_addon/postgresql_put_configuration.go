// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlPutConfiguration `PUT /internal/{id}/configuration` — overwrite the stored per-addon
custom configuration override (internal auth). A body that parses as the
`{"postgresql-config":{"override":{...}}}` wrapper and lands on an
existing row is a bare 204; a well-formed-but-wrong-shape body OR an
unknown addon is a bare 404 (the reference collapses both through its
Option chain); genuinely malformed JSON is a bare 400 (the reference's
`Json` body parser rejects it before the controller ever sees it).

Source: rewrite/rust/src/configuration/controller.rs — put_configuration

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: PostgreSQL addon string id (postgresql_<uuid>)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlPutConfiguration(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_put_configuration
*/
func PostgresqlPutConfiguration(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_put_configuration", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/%s/configuration", id)

	// Make API call
	response := client.Put[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
