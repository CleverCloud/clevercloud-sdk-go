// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlInternalRandomIds `GET .../random-ids?amount=N` — a crawling sample of dedicated,
instantiated, unconfigured addon ids. A repository failure is a 500 whose
body is the driver message as a bare JSON string (the reference renders
the `Left` of its `Either`, not a `{error,hint}` envelope).

Source: rewrite/rust/src/addons/controller.rs — random_ids

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlInternalRandomIds(ctx, client, tracer, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_internal_random_ids
*/
func PostgresqlInternalRandomIds(ctx context.Context, c *client.Client, tracer trace.Tracer, opts ...Option) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_internal_random_ids")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/addon-providers/postgresql-addon/random-ids")

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
