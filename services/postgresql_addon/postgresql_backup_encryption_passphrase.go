// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlBackupEncryptionPassphrase `GET /internal/{id}/encryptionPassphrase` — the decrypted passphrase in
clear text for the backup-manager. Unlike every other addon-password
route in this crate, there is deliberately NO cross-addon IDOR guard here
(`#1416`-class): the reference's `addonBasicAuth` on this specific route
authenticates ANY valid addon credential, not just the path addon's own —
this is the backup-manager's own service account style call, reproduced
as is.

The 404 text carries the reference's double-negative verbatim
("doesn't NOT exist"). Missing key vs missing cipher/nonce are NOT
distinguished (both render the SAME "No passphrase defined" 500) — this
reuses `decrypt_passphrase` exactly as `addon_full_view` does, so a
deployment with no `ENCRYPTION_KEY` configured answers the SAME message a
key-having deployment gives an addon with no passphrase stored, matching
the reference's own `AddonData.encryption_passphrase: Option<String>`
collapsing both cases before this handler ever sees them.

Source: rewrite/rust/src/backup_directory/controller.rs — encryption_passphrase

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: PostgreSQL addon string id (postgresql_<uuid>)

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlBackupEncryptionPassphrase(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_backup_encryption_passphrase
*/
func PostgresqlBackupEncryptionPassphrase(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postgresql_backup_encryption_passphrase", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/internal/%s/encryptionPassphrase", id)

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
