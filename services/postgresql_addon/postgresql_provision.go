// Code generated by generate-services. DO NOT EDIT.

package postgresql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
PostgresqlProvision `POST /resources` — provision a PostgreSQL addon (shared cluster DB, or
dedicated Sdc/DirectHost/LoadBalancer VM).

Body deviation (D-015 class): Play's `parse.json` renders its own error
pages on an unparsable body (400) or a non-JSON content type (415); the
axum rejection carries the same statuses with a plain-text body instead —
the exact deviation `rewrite/rust/src/provisioning/routes.rs`'s own doc
comment records for the frozen Rust port this transplants from.

Source: rewrite/rust/src/provisioning/{routes,controller}.rs — provision

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := postgresql_addon.PostgresqlProvision(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: postgresql_addon
operationId: postgresql_provision
*/
func PostgresqlProvision(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[models.PostgresqlProvisionResponse] {
	ctx, span := tracer.Start(ctx, "postgresql_provision")
	defer span.End()

	path := utils.Path("/v2/providers/postgresql-addon/resources")

	// Make API call
	response := client.Post[models.PostgresqlProvisionResponse](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
