// Code generated by generate-services. DO NOT EDIT.

package auth

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Getloginform1 GET /v2/sessions/login — login page and account-deletion trigger (plural alias).

Source: cc-api SessionAPI.java — /sessions/login aliases /session/login
Issue: #99, #2484

📥 **Algo Source (Legacy):** identical to `/v2/session/login` — the plural
path is the same resource, so it resolves a `deletionKey` on the same terms.

🔧 **Algo Rust (Implementation):** delegates to [`get_session_login`], so the
credential-less deletion trigger is reachable here too — the confirmation
mail builds the singular URL, but both spellings must behave identically.

Legacy: cc-api SessionsAPI.java — plural alias for /session/login
Conformity: faithful — exact alias

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := auth.Getloginform1(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: auth
operationId: getLoginForm_1
*/
func Getloginform1(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "getLoginForm_1")
	defer span.End()

	path := utils.Path("/v2/sessions/login")

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
