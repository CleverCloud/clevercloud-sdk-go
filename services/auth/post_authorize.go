// Code generated by generate-services. DO NOT EDIT.

package auth

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Postauthorize POST /v2/authorize — check authorization for a given action.

The handler is only the outer three steps of legacy's `authorize()`: validate
the body, authenticate the caller, hand everything to the dispatch engine and
return what it says. The URI parsing, the path split, the query-string map, the
v2/v4 routers and the answer bean all live in [`crate::authorize_engine`],
which is also where each arm is ported.

**All eleven `/v2` roots are ported** — `accesslogs`, `backups`,
`certificates`, `events`, `legacy-logs`/`logs`, `metrics`, `notifications`,
`providers`, `saas` (which `/v4/saas` shares), `users` and `w10tokens`. The
`users` chain answers `400 "no such endpoint"` for every input, because it does
so in legacy: three consecutive `case "applications"` arms end in a lookup on
that literal.

**Every `/v4` root is ported too**, along with both subtrees below them — the
`authorize_v4_organisation` chain and `authorize_v4_vendor` with the two
methods it dispatches into. No root of either half declines where legacy
answers.

**A 200 body is not one shape**, and one arm answers two of them. The v2
subtree answers, depending on the arm, no entity at all (`backups`,
`certificates`, and the admin and direct-grant short-circuits of the `users`
leaf), a serialised `UserView` (`metrics/read`, `w10tokens`, `notifications`,
`providers`, `saas`, and that same `users` leaf's last outcome), an ad-hoc
`{"organisation_id": …}` object (`logs`, `accesslogs`), a JSON **string
literal** (`events`, for a caller that is not EXTERNAL) or the whole
`/v2/summary` document (`events`, for one that is — the arm that answers two
shapes). None
of them answers an `AuthAnswer`: that bean appears only on the `400 "no such
endpoint"` fallthrough. Some 400s carry no entity either — a body missing
`verb`/`uri`/`authorization`, and the two refusals `authorize_hook_access`
answers for a payload it cannot bind or a scope entry it does not recognise.

Source: cc-api AuthorizationApi.java:28,38,42-47 authorize()
Source: cc-api AuthorizationHelper.java:82-123 authorize()
Issue: #99
Issues: refs #1310

Algorithm: Parse WannabeAuthorization, verify authentication, run the dispatch engine; a v4 root with no ported arm takes the legacy `no such endpoint` refusal
Legacy: cc-api AuthorizationApi.java authorize() -> AuthorizationHelper.authorize()
Conformity: faithful on the frame, the fallthrough and all eleven v2 roots, permissive corners, dead routes and uncaught-exception 500s included

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := auth.Postauthorize(ctx, client, tracer, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: auth
operationId: postAuthorize
*/
func Postauthorize(ctx context.Context, c *client.Client, tracer trace.Tracer, requestBody *models.WannabeAuthorization) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "postAuthorize")
	defer span.End()

	path := utils.Path("/v2/authorize")

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
