// Code generated by generate-services. DO NOT EDIT.

package internalapi

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
EnsureOrgDependencies `POST /v2/internal/organisations/{id}/ensureDependencies` — satisfy the owner's boot
dependencies, provisioning what is missing.

**One legacy endpoint, one implementation.** cc-api declares this in
`InternalOrganisationsAPI`, so it belongs to this crate even though the work it does —
provisioning the owner's Pulsar system addons — lives in `addons`. That module is reached
as a library through `OrganisationsModule::addons()`, which is exactly how the sibling
mount at the bare `/internal/…` prefix already reaches it. Serving it *from* `addons`
instead put an organisations-surface route in another crate's ownership zone and split one
legacy endpoint across two crates, which is how it ended up mounted twice and panicking
`Router::merge` at boot (#2373).

The bare `/internal/…` twin this used to delegate to is gone (#2404): legacy never served
that prefix, so it had no caller — and it carried a weaker gate than this one.

Source: cc-api InternalOrganisationsAPI.java:130-143 checkOrganisationBootDependencies
Behavior: 404 when the org is unknown, 412 when a dependency could not be satisfied,

	200 otherwise.

Issues: refs #2373

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: Organisation ID

# Returns the operation result or an error

Example:

	response := internalapi.EnsureOrgDependencies(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: internal
operationId: ensure_org_dependencies
*/
func EnsureOrgDependencies(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "ensure_org_dependencies", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/internal/organisations/%s/ensureDependencies", id)

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
