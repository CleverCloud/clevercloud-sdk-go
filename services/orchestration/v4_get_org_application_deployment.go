// Code generated by generate-services. DO NOT EDIT.

package orchestration

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
V4GetOrgApplicationDeployment Legacy: Console3 v4 orchestration (no cc-api equivalent — OVD OrchestrationRoutes)
Algorithm: Shape S1 authorization and the same row as the v2 route, rendered as the v4 DeploymentView
Conformity: faithful shape, partial data — see the list route

GET /v4/orchestration/organisations/{id}/applications/{app_id}/deployments/{deployment_id}

`{deployment_id}` is the deployment UUID, which is what the v4 body's `id`
carries — not the numeric per-application `real_id` the v2 body calls `id`.
The lookup is UUID-only, as the v2 handler's is.

Source: ovd core/.../orchestration/models/Deployment.scala:100 (DeploymentView)
Spec: analysis-output/38-github-api-consumers.md §Console3 — v4 orchestration
Issue: #313

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: Organisation ID
  - app_id: Application ID
  - deployment_id: Deployment ID

# Returns the operation result or an error

Example:

	response := orchestration.V4GetOrgApplicationDeployment(ctx, client, tracer, id, app_id, deployment_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: orchestration
operationId: v4_get_org_application_deployment
*/
func V4GetOrgApplicationDeployment(ctx context.Context, c *client.Client, tracer trace.Tracer, id string, app_id string, deployment_id string) client.Response[models.V4DeploymentView] {
	ctx, span := tracer.Start(ctx, "v4_get_org_application_deployment", trace.WithAttributes(attribute.String("id", id), attribute.String("app_id", app_id), attribute.String("deployment_id", deployment_id)))
	defer span.End()

	path := utils.Path("/v4/orchestration/organisations/%s/applications/%s/deployments/%s", id, app_id, deployment_id)

	// Make API call
	response := client.Get[models.V4DeploymentView](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
