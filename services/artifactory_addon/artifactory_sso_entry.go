// Code generated by generate-services. DO NOT EDIT.

package artifactory_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
ArtifactorySsoEntry POST /v2/providers/artifactory/sso — SSO entry point for Artifactory addon dashboard.

**Legacy**: addon-api-artifactory DashboardController.scala ssoEntryPoint()
**Algorithm**:
  - Validates HMAC token using SSO salt, checks timestamp freshness.
  - Looks up addon by string ID, returns 400 if not found.
  - Creates session token for dashboard access.

**Conformity**: YES (Cloud Versatile conditional — works when configured)

Source: references/legacy/addon-api/artifactory/app/controllers/DashboardController.scala — ssoEntryPoint()
Behavior: Validates HMAC token using SSO salt, verifies timestamp freshness,

	looks up addon by string ID, then redirects to /dashboard with session cookie.
	Returns 501 if SSO salt not configured (Cloud Versatile).

Issue: #TBD

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := artifactory_addon.ArtifactorySsoEntry(ctx, client, tracer, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: artifactory_addon
operationId: artifactory_sso_entry
*/
func ArtifactorySsoEntry(ctx context.Context, c *client.Client, tracer trace.Tracer, requestBody *models.SsoRequest) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "artifactory_sso_entry")
	defer span.End()

	path := utils.Path("/v2/providers/artifactory/sso")

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
