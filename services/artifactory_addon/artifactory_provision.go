// Code generated by generate-services. DO NOT EDIT.

package artifactory_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
ArtifactoryProvision POST /v2/providers/artifactory/resources — Provision a new Artifactory addon.

**Legacy**: addon-api-artifactory ProvisionController.scala provision()
**Algorithm**:
  - Returns 501 (Cloud Versatile) — requires CC-API to create Jenkins app, PG addon,
    FSBucket, DNS entry, and git push of Artifactory repo.

**Conformity**: YES (Cloud Versatile conditional — works when configured)

Source: references/legacy/addon-api/artifactory/app/controllers/ProvisionController.scala — provision()
Behavior: Creates a compound addon (Jenkins app + PostgreSQL + FSBucket + DNS).

	Returns 501 when infrastructure provisioning is not configured (Cloud Versatile).
	In full mode: creates CC app, PG addon, FSBucket, DNS entry, git push, then inserts row.

Issue: #TBD

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := artifactory_addon.ArtifactoryProvision(ctx, client, tracer, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: artifactory_addon
operationId: artifactory_provision
*/
func ArtifactoryProvision(ctx context.Context, c *client.Client, tracer trace.Tracer, requestBody *models.ArtifactoryProvisionRequest) client.Response[models.ArtifactoryProvisionResponse] {
	ctx, span := tracer.Start(ctx, "artifactory_provision")
	defer span.End()

	path := utils.Path("/v2/providers/artifactory/resources")

	// Make API call
	response := client.Post[models.ArtifactoryProvisionResponse](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
