// Code generated by generate-services. DO NOT EDIT.

package artifactory_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
ArtifactoryDeprovision DELETE /v2/providers/artifactory/resources/{id} — Deprovision an Artifactory addon.

**Legacy**: addon-api-artifactory ProvisionController.scala deprovision()
**Algorithm**:
  - Looks up addon by string ID, returns 404 if not found.
  - Marks addon as DELETED (soft-delete with deletion_date).
  - Full infra cleanup (DNS, apps, PG, FSBucket) deferred to async worker.

**Conformity**: PARTIAL

Source: references/legacy/addon-api/artifactory/app/controllers/ProvisionController.scala — deprovision()
Behavior: Looks up addon by string ID ("artifactory_{uuid}"), then:
 1. Unregisters DNS hostname
 2. Deletes the CC application, FSBucket addon, PostgreSQL addon
 3. Hard-deletes the provision row
    Returns 404 if addon not found, 501 if infra not configured.

Issue: #TBD

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: Addon string ID (artifactory_{uuid})

# Returns the operation result or an error

Example:

	response := artifactory_addon.ArtifactoryDeprovision(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: artifactory_addon
operationId: artifactory_deprovision
*/
func ArtifactoryDeprovision(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "artifactory_deprovision", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/artifactory/resources/%s", id)

	// Make API call
	response := client.Delete[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
