// Code generated by generate-services. DO NOT EDIT.

package open_telemetry

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Deleteotelgateway DELETE /v4/otel/gateways/{gateway_id} — unregister a gateway (platform
operators only). 204. **No cascade**: 409 while any namespace still uses
the gateway (active, or soft-deleted and awaiting its gateway-side
cleanup). A soft delete: the row is kept and stamped `deleted_at`, the
gateway leaves the list and the get, and its `(location, endpoint)` can be
registered again. Nothing is sent to the gateway itself.

Source: GitLab issue #3584 (no OVD counterpart — OVD has no gateway delete)
Issue: #3584

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - gateway_id: Gateway id (otelgw_<uuid>)

# Returns the operation result or an error

Example:

	response := open_telemetry.Deleteotelgateway(ctx, client, tracer, gateway_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: OpenTelemetry
operationId: deleteOtelGateway
*/
func Deleteotelgateway(ctx context.Context, c *client.Client, tracer trace.Tracer, gateway_id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "deleteOtelGateway", trace.WithAttributes(attribute.String("gateway_id", gateway_id)))
	defer span.End()

	path := utils.Path("/v4/otel/gateways/%s", gateway_id)

	// Make API call
	response := client.Delete[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
