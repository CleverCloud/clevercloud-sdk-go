// Code generated by generate-services. DO NOT EDIT.

package my_sql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
MysqlPatchSharedCluster PATCH /internal/shared/cluster/{id} — update availability and/or backup
routing. The backup pair writes only when BOTH fields arrive parseable; a
half pair (or an unparsable instance uuid) is silently ignored, matching
Play, which built that BadRequest and then DISCARDED it, always answering
204 (Internal.scala:335, :341). Returning the 400 here was an early return
that also dropped the availability update below — the only writer of
`cluster.available` (#2557 item 6). The 400 in the responses is the
non-uuid path id.
Source: Internal.patchSharedCluster.

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: cluster uuid
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := my_sql_addon.MysqlPatchSharedCluster(ctx, client, tracer, id, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: my_sql_addon
operationId: mysql_patch_shared_cluster
*/
func MysqlPatchSharedCluster(ctx context.Context, c *client.Client, tracer trace.Tracer, id string, requestBody *models.WannaPatchCluster) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "mysql_patch_shared_cluster", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/mysql-addon/internal/shared/cluster/%s", id)

	// Make API call
	response := client.Patch[client.Nothing](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
