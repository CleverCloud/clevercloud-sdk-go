// Code generated by generate-services. DO NOT EDIT.

package my_sql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
MysqlGetEncryptionPassphrase GET /internal/{id}/encryptionPassphrase — the decrypted disk passphrase for
the booting VM (per-addon Basic auth). Binds the authenticated addon to the
requested `{id}` so a foreign addon's valid password cannot disclose this
addon's passphrase — the legacy IDOR, fixed here (#714, see redis #1416).

Source: Internal.scala getEncryptionPassphrase.

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - id: provision uuid

# Returns the operation result or an error

Example:

	response := my_sql_addon.MysqlGetEncryptionPassphrase(ctx, client, tracer, id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: my_sql_addon
operationId: mysql_get_encryption_passphrase
*/
func MysqlGetEncryptionPassphrase(ctx context.Context, c *client.Client, tracer trace.Tracer, id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "mysql_get_encryption_passphrase", trace.WithAttributes(attribute.String("id", id)))
	defer span.End()

	path := utils.Path("/v2/providers/mysql-addon/internal/%s/encryptionPassphrase", id)

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
