// Code generated by generate-services. DO NOT EDIT.

package my_sql_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	trace "go.opentelemetry.io/otel/trace"
)

/*
MysqlSsoEntryPoint POST /sso — the SSO entry point. Validates the HMAC-SHA512 SSO signature
(self-authenticating, no broker/session auth), mints a Sessioned token and
serves an HTML page redirecting to `dashboard?token=…` (relative, so it
resolves under both the bare and `/v2/...` layouts).

Source: Dashboard.scala ssoEntryPoint + addonprovider-lib SsoAuth/SessionAuth.

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := my_sql_addon.MysqlSsoEntryPoint(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: my_sql_addon
operationId: mysql_sso_entry_point
*/
func MysqlSsoEntryPoint(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "mysql_sso_entry_point")
	defer span.End()

	path := utils.Path("/v2/providers/mysql-addon/sso")

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
