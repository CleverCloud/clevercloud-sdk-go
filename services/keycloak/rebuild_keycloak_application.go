// Code generated by generate-services. DO NOT EDIT.

package keycloak

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Rebuildkeycloakapplication POST /v4/addon-providers/addon-keycloak/addons/{keycloak_id}/rebuild

📥 **Algo Source (Legacy):** the same `rebootAddonKeycloak` with
`rebuild = true` (`A:871`) — redeploys **without** the build cache, so the
FSBucket themes/plugins are copied again.

⚠ reboot and rebuild differ **only** by this boolean, and the legacy suite never
varies it — a port that swapped it would pass every legacy test
(`01-legacy-tests.md`, SMOKE-ONLY finding). The dedicated regression test for
this pair is in `tests/provision_tests.rs`.

Source: references/legacy/ovd/modules/keycloak/src/main/scala/com/clevercloud/keycloak/actors/AddonKeycloakAddonActor.scala:871
Issues: #313

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - addonKeycloakId: Keycloak addon id

# Returns the operation result or an error

Example:

	response := keycloak.Rebuildkeycloakapplication(ctx, client, tracer, addonKeycloakId)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: keycloak
operationId: rebuildKeycloakApplication
*/
func Rebuildkeycloakapplication(ctx context.Context, c *client.Client, tracer trace.Tracer, addonKeycloakId string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "rebuildKeycloakApplication", trace.WithAttributes(attribute.String("addonKeycloakId", addonKeycloakId)))
	defer span.End()

	path := utils.Path("/v4/addon-providers/addon-keycloak/addons/%s/rebuild", addonKeycloakId)

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
