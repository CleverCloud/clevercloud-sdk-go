// Code generated by generate-services. DO NOT EDIT.

package cellar

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Createuploadurl Presign a direct S3 upload URL

Returns `{ url, expiresAt }`: a presigned S3 URL the caller PUTs the object bytes to, straight to the storage cluster. `expiresIn` sets its lifetime in seconds (300 by default) and is used as given, with no clamping. The target object need not exist yet, but the bucket must. The caller must be authenticated and must be the add-on's owner (personal add-ons) or a member of the organisation in the path. An unknown add-on or bucket answers 404.

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - ownerId: Owner (org) ID
  - CellarId: Cellar addon ID
  - bucketName: Bucket name
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := cellar.Createuploadurl(ctx, client, tracer, ownerId, CellarId, bucketName, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: cellar
operationId: createUploadUrl
*/
func Createuploadurl(ctx context.Context, c *client.Client, tracer trace.Tracer, ownerId string, CellarId string, bucketName string, requestBody *models.SignedUrlRequest) client.Response[models.SignedUrlResponse] {
	ctx, span := tracer.Start(ctx, "createUploadUrl", trace.WithAttributes(attribute.String("ownerId", ownerId), attribute.String("CellarId", CellarId), attribute.String("bucketName", bucketName)))
	defer span.End()

	path := utils.Path("/v4/cellar/organisations/%s/cellar/%s/buckets/%s/objects/upload-url", ownerId, CellarId, bucketName)

	// Make API call
	response := client.Post[models.SignedUrlResponse](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
