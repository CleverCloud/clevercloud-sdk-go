// Code generated by generate-services. DO NOT EDIT.

package addons

import (
	"fmt"
	"net/url"
	"strings"
)

// Option defines a functional option for addons operations
type Option func(*Options)

// Options holds query parameters for addons operations
type Options struct {
	Planid *string `url:"planId,omitempty"`
}

// WithPlanid sets the planId query parameter
func WithPlanid(planId string) Option {
	return func(o *Options) {
		o.Planid = &planId
	}
}

// buildQueryString builds a query string from options
func buildQueryString(opts ...Option) string {
	options := &Options{}
	for _, opt := range opts {
		opt(options)
	}

	var params []string
	if options.Planid != nil {
		params = append(params, fmt.Sprintf("planId=%s", url.QueryEscape(*options.Planid)))
	}

	if len(params) == 0 {
		return ""
	}
	return strings.Join(params, "&")
}
