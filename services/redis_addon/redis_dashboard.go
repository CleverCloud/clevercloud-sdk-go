// Code generated by generate-services. DO NOT EDIT.

package redis_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	trace "go.opentelemetry.io/otel/trace"
)

/*
RedisDashboard GET /v2/providers/redis-addon/dashboard — Render the Redis dashboard.

**Legacy**: addon-api-redis Dashboard.scala dashboard()
**Algorithm**:
  - Extracts session token from `Authorization: CC <token>` header or `?token=` query.
  - Verifies session token (XSalsa20Poly1305 decryption, 24h expiry check).
  - Fetches addon from DB and returns dashboard state JSON.
  - 501 only when ENCRYPTION_KEY not configured (Cloud Versatile).

**Conformity**: YES (Cloud Versatile conditional 501 when config missing)

Source: references/legacy/addon-api/redis/app/controllers/Dashboard.scala — dashboard
Behavior: verifies session token, returns addon dashboard state JSON
Issue: #713

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := redis_addon.RedisDashboard(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: redis_addon
operationId: redis_dashboard
*/
func RedisDashboard(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "redis_dashboard")
	defer span.End()

	path := utils.Path("/v2/providers/redis-addon/dashboard")

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
