// Code generated by generate-services. DO NOT EDIT.

package redis_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
RedisAbortMigration DELETE /v2/providers/redis-addon/resources/{addon_id}/migrations/{migration_id} — Abort a migration.

**Legacy**: addon-api-redis Application.scala abortMigration()
**Algorithm**:
  - Parses the migration UUID and looks the migration up.
  - Delegates to the shared abort dispatch: `RUNNING` (last step
    DUMP_AND_RESTORE/RUNNING) mirrors Scala `migrationUpdate(DUMP_RESTORE_ABORT)`;
    `RECOVERING` is the net-new client abort (#1429) ending `ABORTED`; a
    terminal migration yields 400.

**Conformity**: YES for the RUNNING path; the RECOVERING path is net-new (#1429).

Source: references/legacy/addon-api/redis/app/controllers/Application.scala — abortMigration
Behavior: 200 on abort (RUNNING or RECOVERING); 400 on unparseable UUID,

	unknown migration, terminal migration (NoMigrationRunningForThisId), or a
	RUNNING migration with the wrong last step
	(MigrationMustBeInDumpAndRestoreToBeAborted); 500 on DB error.

Issue: #713, #1173, clever-cloud/axo#1249, clever-cloud/axo#1429

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - addon_id: Redis addon ID
  - migration_id: Migration ID

# Returns the operation result or an error

Example:

	response := redis_addon.RedisAbortMigration(ctx, client, tracer, addon_id, migration_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: redis_addon
operationId: redis_abort_migration
*/
func RedisAbortMigration(ctx context.Context, c *client.Client, tracer trace.Tracer, addon_id string, migration_id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "redis_abort_migration", trace.WithAttributes(attribute.String("addon_id", addon_id), attribute.String("migration_id", migration_id)))
	defer span.End()

	path := utils.Path("/v2/providers/redis-addon/resources/%s/migrations/%s", addon_id, migration_id)

	// Make API call
	response := client.Delete[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
