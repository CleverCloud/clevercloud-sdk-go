// Code generated by generate-services. DO NOT EDIT.

package redis_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
RedisSsoEntryPoint POST /v2/providers/redis-addon/sso — SSO entry point from Clever Cloud console.

**Legacy**: addon-api-redis Dashboard.scala ssoEntryPoint()
**Algorithm**:
  - Validates SHA-512 signature using sso_salt.
  - Creates encrypted session token (XSalsa20Poly1305) for dashboard access.
  - Redirects to the dashboard with the token URL-encoded in the `?token=`
    query (200 meta-refresh landing page, #1438).
  - 501 only when SSO_SALT or ENCRYPTION_KEY not configured (Cloud Versatile).

**Conformity**: YES (Cloud Versatile conditional 501 when config missing)

Source: references/legacy/addon-api/redis/app/controllers/Dashboard.scala — ssoEntryPoint
Source: addonprovider-lib SsoAuth.scala — checkSsoRequest (SHA-512 signature)
Behavior: validates SSO signature, creates session token, redirects to the dashboard
Issue: #713, #1438

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := redis_addon.RedisSsoEntryPoint(ctx, client, tracer, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: redis_addon
operationId: redis_sso_entry_point
*/
func RedisSsoEntryPoint(ctx context.Context, c *client.Client, tracer trace.Tracer, requestBody *models.SsoRequest) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "redis_sso_entry_point")
	defer span.End()

	path := utils.Path("/v2/providers/redis-addon/sso")

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
