// Code generated by generate-services. DO NOT EDIT.

package redis_addon

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
RedisEncryptAddon POST /v2/providers/redis-addon/resources/{addon_id}/encrypt — Enable encryption on an addon.

**Legacy**: addon-api-redis Application.scala encryptAddon()
**Algorithm**:
  - Generates a 30-char alphanumeric passphrase (legacy shape), encrypts with XSalsa20Poly1305 using ENCRYPTION_KEY.
  - Stores cipher+nonce in DB (encryption_passphrase_cipher, encryption_passphrase_nonce).
  - Returns error if key already exists (idempotency guard).
  - 501 only when ENCRYPTION_KEY not configured (Cloud Versatile).

**Conformity**: YES (Cloud Versatile conditional 501 when config missing)

Source: references/legacy/addon-api/redis/app/controllers/Application.scala — encryptAddon
Behavior: generates encrypted passphrase, stores in DB, returns addon config
Issue: #713

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - addon_id: Redis addon ID

# Returns the operation result or an error

Example:

	response := redis_addon.RedisEncryptAddon(ctx, client, tracer, addon_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: redis_addon
operationId: redis_encrypt_addon
*/
func RedisEncryptAddon(ctx context.Context, c *client.Client, tracer trace.Tracer, addon_id string) client.Response[models.ProvisionResponse] {
	ctx, span := tracer.Start(ctx, "redis_encrypt_addon", trace.WithAttributes(attribute.String("addon_id", addon_id)))
	defer span.End()

	path := utils.Path("/v2/providers/redis-addon/resources/%s/encrypt", addon_id)

	// Make API call
	response := client.Post[models.ProvisionResponse](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
