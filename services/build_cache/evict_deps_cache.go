// Code generated by generate-services. DO NOT EDIT.

package build_cache

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
EvictDepsCache Evict the dependencies archive.

Same contract as [`evict_build_cache`], against the deps key — which carries no
commit, so this removes the archive shared by every commit of the application
at that BAS version. `commit_id` is still taken from the path because the
legacy route binds it and builds the same `BuildDefinition`; it does not affect
the key.

📥 **Algo Source (Legacy):** as `delete_build_cache`, but keyed on
`getDepsCacheFilename`.
Source: build-cache-api app/controllers/Application.scala:278-281

🔧 **Algo Rust (Implementation):** the same `evict` body with
`CacheKind::Deps`, which selects [`BuildDefinition::deps_key`].

Behavior: `204` on success or on an already-absent archive, `401` without the

	SERVICE pair, `500` on any other storage failure.

Issue: #1652

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - app_id: Application id
  - commit_id: Bound by the route; not part of the deps key
  - bas_version: BAS version, `{type}-{version}`

# Returns the operation result or an error

Example:

	response := build_cache.EvictDepsCache(ctx, client, tracer, app_id, commit_id, bas_version)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: build_cache
operationId: evict_deps_cache
*/
func EvictDepsCache(ctx context.Context, c *client.Client, tracer trace.Tracer, app_id string, commit_id string, bas_version string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "evict_deps_cache", trace.WithAttributes(attribute.String("app_id", app_id), attribute.String("commit_id", commit_id), attribute.String("bas_version", bas_version)))
	defer span.End()

	path := utils.Path("/api/builds/deps-cache/%s/%s/%s", app_id, commit_id, bas_version)

	// Make API call
	response := client.Delete[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
