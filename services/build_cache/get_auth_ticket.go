// Code generated by generate-services. DO NOT EDIT.

package build_cache

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
GetAuthTicket Mint a per-deploy ticket (GET form).

A thin wrapper so the routed `GET` verb is visible to the OpenAPI spec:
`#[utoipa::path]` documents one method per annotation, and a routed-but-
unannotated verb is exactly the "invisible endpoint" devel/api/CLAUDE.md
principle 11 forbids. Behaviour is `create_auth_ticket`'s, unchanged.

The legacy routes both verbs (`conf/routes:15-16`) and BDS uses POST; this
exists for parity with callers that were written against the GET form.

📥 **Algo Source (Legacy):** identical to the POST form — `conf/routes:15-16`
binds both verbs to the same `createAuthTicket` action.

🔧 **Algo Rust (Implementation):** delegates to [`create_auth_ticket`]; exists
only so the routed verb appears in the OpenAPI document.

Source: build-cache-api conf/routes:15-16 — `GET /api/tickets/…`
Behavior: identical to the POST form; see [`create_auth_ticket`].
Issue: #1652

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - app_id: Application id
  - commit_id: Deployed commit
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := build_cache.GetAuthTicket(ctx, client, tracer, app_id, commit_id, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: build_cache
operationId: get_auth_ticket
*/
func GetAuthTicket(ctx context.Context, c *client.Client, tracer trace.Tracer, app_id string, commit_id string, opts ...Option) client.Response[models.TicketDefinition] {
	ctx, span := tracer.Start(ctx, "get_auth_ticket", trace.WithAttributes(attribute.String("app_id", app_id), attribute.String("commit_id", commit_id)))
	defer span.End()

	path := utils.Path("/api/tickets/%s/%s", app_id, commit_id)

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[models.TicketDefinition](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
