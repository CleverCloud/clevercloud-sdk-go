// Code generated by generate-services. DO NOT EDIT.

package build_cache

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Complete `PUT /multipart/done` — assemble the parts into the final object.

📥 **Algo Source (Legacy):**
The call that actually creates the archive: until it lands, the parts exist but
no object does.
- Verify the macaroon as above
- `CompleteMultipartUpload` with the reported `{PartNumber, ETag}` list
- Source: clever-cloud/multipart-uploader uploader.go:178-207

🔧 **Algo Rust (Implementation):**
- `authorise()` → macaroon, then bucket/key agreement
- 500 when `uploadId` or `parts` is absent
- `storage::complete_multipart_upload`, then a best-effort HEAD for the size
- `stats::record(UploadCompleted, size)` — best-effort, never fails the upload
- Return 200 with an empty body (the client ignores it)

Source: clever-cloud/multipart-uploader uploader.go:178-207
Schema: crates/api/build-cache/migrations/20260805010944_create_build_cache_events.sql
Issue: #1652

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := build_cache.Complete(ctx, client, tracer, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: build_cache
operationId: complete
*/
func Complete(ctx context.Context, c *client.Client, tracer trace.Tracer, requestBody *models.MultipartRequest) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "complete")
	defer span.End()

	path := utils.Path("/api/build-cache/proxy/multipart/done")

	// Make API call
	response := client.Put[client.Nothing](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
