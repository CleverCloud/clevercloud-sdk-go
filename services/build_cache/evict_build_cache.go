// Code generated by generate-services. DO NOT EDIT.

package build_cache

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
EvictBuildCache Evict the build archive.

📥 **Algo Source (Legacy):** `Authenticated`, then `deleteCacheObjects` on
`build.getFilename`: `s3Client.deleteObject` → `NoContent`; an
`AmazonS3Exception` with status 404 → `NoContent` as well; anything else is
logged and returns `InternalServerError`.
Source: build-cache-api app/controllers/Application.scala:273-276

🔧 **Algo Rust (Implementation):** `verify_service_credentials`, resolve the
Cellar, `storage::delete_archive` on [`BuildDefinition::build_key`], `204`.
The absent-object case is folded into the helper, so this returns `204`
whether or not the archive was there — matching the legacy.

Behavior: `204` on success or on an already-absent archive, `401` without the

	SERVICE pair, `500` on any other storage failure.

Issue: #1652

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - app_id: Application id
  - commit_id: Deployed commit
  - bas_version: BAS version, `{type}-{version}`

# Returns the operation result or an error

Example:

	response := build_cache.EvictBuildCache(ctx, client, tracer, app_id, commit_id, bas_version)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: build_cache
operationId: evict_build_cache
*/
func EvictBuildCache(ctx context.Context, c *client.Client, tracer trace.Tracer, app_id string, commit_id string, bas_version string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "evict_build_cache", trace.WithAttributes(attribute.String("app_id", app_id), attribute.String("commit_id", commit_id), attribute.String("bas_version", bas_version)))
	defer span.End()

	path := utils.Path("/api/builds/%s/%s/%s", app_id, commit_id, bas_version)

	// Make API call
	response := client.Delete[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
