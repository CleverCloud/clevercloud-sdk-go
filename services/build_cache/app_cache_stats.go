// Code generated by generate-services. DO NOT EDIT.

package build_cache

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
AppCacheStats Read one application's build-cache usage.

📖 **Algo Source (Legacy):** none — build-cache-api records nothing. Whether a
deploy used its cache is observable there only by reading the build VM's log
for rubydeployer's `A build cache archive exists, downloading in progress…`
(`buildcache.rb:115`), which requires access to the VM and outlives nothing.

🔧 **Algo Rust (Implementation):**
  - `verify_service_credentials` — the same pair that guards the mint endpoint
  - `stats::for_app` groups `build_cache_events` by object key and sums the
    per-event counters in SQL

The counts are of DECISIONS THIS MODULE MADE, not of transfers. A `hit` means
a presigned GET was served; whether the guest then fetched it happens between
the guest and Cellar, and nothing in this process sees it. Reporting it as
"downloads" would be a number we cannot stand behind.

Source: crates/api/build-cache/migrations/20260805010944_create_build_cache_events.sql
Behavior: service-authenticated. Always `200` for a well-formed request —

	an application with no recorded activity reports zeroes and an empty
	`archives` list, which is the honest answer and distinguishable from
	an error. `401` on a bad service pair, `500` on a registry failure.

Issue: #1652

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - app_id: Application id

# Returns the operation result or an error

Example:

	response := build_cache.AppCacheStats(ctx, client, tracer, app_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: build_cache
operationId: app_cache_stats
*/
func AppCacheStats(ctx context.Context, c *client.Client, tracer trace.Tracer, app_id string) client.Response[models.AppCacheStats] {
	ctx, span := tracer.Start(ctx, "app_cache_stats", trace.WithAttributes(attribute.String("app_id", app_id)))
	defer span.End()

	path := utils.Path("/api/build-cache/stats/%s", app_id)

	// Make API call
	response := client.Get[models.AppCacheStats](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
