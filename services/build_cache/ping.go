// Code generated by generate-services. DO NOT EDIT.

package build_cache

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Ping `POST /ping` — the watchdog echo.

📥 **Algo Source (Legacy):** `Action(parse.json)`; the body is read as a
`Ping`, and on success answers `Ok(Json(ping.copy(action = "PONG")))` — the
same object with `action` replaced and `data` untouched. A body that does not
bind yields `BadRequest`.
Source: build-cache-api app/controllers/Application.scala:142-146

🔧 **Algo Rust (Implementation):** `Json<Ping>` extraction; on success rebuild
with `action = "PONG"` and the request's `data`. The rejection is mapped to
`400` explicitly — axum's default for a malformed body is `422`, which would
have been a silent divergence from the legacy for every malformed probe.

Behavior: `200` with `{"action":"PONG","data":<echoed>}`; `400` on a body that

	is not an object with both string fields. No authentication.

Issue: #1652

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - requestBody: the request payload

# Returns the operation result or an error

Example:

	response := build_cache.Ping(ctx, client, tracer, requestBody)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: build_cache
operationId: ping
*/
func Ping(ctx context.Context, c *client.Client, tracer trace.Tracer, requestBody *models.Ping) client.Response[models.Ping] {
	ctx, span := tracer.Start(ctx, "ping")
	defer span.End()

	path := utils.Path("/ping")

	// Make API call
	response := client.Post[models.Ping](ctx, c, path, requestBody)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
