// Code generated by generate-services. DO NOT EDIT.

package misc

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	trace "go.opentelemetry.io/otel/trace"
)

/*
GetEventsStream GET /v2/events — real-time event stream (Server-Sent Events).

📥 **Algo Source (Legacy):**
Subscribe to broadcast channel, stream events via SSE (text/event-stream), filter by ?types= comma-separated event types; return 501 if no event sender configured.
  - Legacy note: cc-api has no `events` root at all — the service that served
    live events was event-api, and its only route was the WebSocket
    `GET /v2/events/event-socket`. This SSE surface is axo-native, so it has no
    wire-shape oracle; what it DOES inherit is event-api's owner scoping.

🔧 **Algo Rust (Implementation):**
- Conformity: stub
Source: event-api WsEventActor.scala:59-74 (owner scoping), OwnerIdAgent.scala:21-23
Source: console3 events-notifications/main.js:175 — EventsStream consumer
Spec: analysis-output/35-console3-api-consumption.md §3.A Server-Sent Events
Spec: analysis-output/livrables/LIVRABLE-B-Architecture.md §B.2.10
Behavior: real broadcast-channel SSE stream; returns 503 only if the

	clever.events fanout consumer failed to start at boot.
	Event types: ADDON_CREATION, ADDON_DELETION, APPLICATION_CREATION,
	APPLICATION_DELETION, APPLICATION_EDITION, APPLICATION_REDEPLOY,
	APPLICATION_STOP, DEPLOYMENT_ACTION_BEGIN, DEPLOYMENT_ACTION_CANCEL,
	DEPLOYMENT_ACTION_END, DEPLOYMENT_FAIL, DEPLOYMENT_SUCCESS.

Audit note (issue #858, OVD MR !1834): this handler is broadcast-channel
backed today (no Pulsar). When we wire Pulsar org-event topics into the
event sender, the implementation MUST address the four bugs OVD !1834
fixed: (1) load Pulsar config via PulsarConfig::from_env_required so the
binary fails fast at boot, (2) pass the biscuit token to any
list_persistent_topics call (modules/pulsar/src/pulsar_admin.rs already
accepts the token argument), (3) drop topics whose schema is incompatible
before subscribing, (4) ensure the consumer is dropped (and any non-durable
subscription cleaned up) on SSE disconnect — not a leak today because
broadcast::Receiver dropping is local.
Issue: #298, #858

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := misc.GetEventsStream(ctx, client, tracer, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: misc
operationId: get_events_stream
*/
func GetEventsStream(ctx context.Context, c *client.Client, tracer trace.Tracer, opts ...Option) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "get_events_stream")
	defer span.End()

	path := utils.Path("/v2/events")

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
