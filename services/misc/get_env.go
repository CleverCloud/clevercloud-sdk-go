// Code generated by generate-services. DO NOT EDIT.

package misc

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Getenv GET /v2/application/{app_id}/environment — legacy environment endpoint.

📥 **Algo Source (Legacy):**
Auth via per-app env_token query param (NOT OAuth1), validate app exists and token matches, return env vars as HashMap from application_environment table.
- Legacy note: Deprecated path — auth is per-app token, not OAuth1. Use /v2/self/applications/{appId}/env instead.

🔧 **Algo Rust (Implementation):**
- Conformity: faithful
Source: cc-api ApplicationEnvironmentAPI.java:42 getEnv() — public API, NO OAuth
Auth: per-app `env_token` query parameter (NOT OAuth1)
Source: cc-api ApplicationEnvironmentHelper.java:98 getEnvironment(appId, envToken)
Deprecated path: use /v2/self/applications/{appId}/env instead.

Issue: #110, #467

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - appId: Application ID
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := misc.Getenv(ctx, client, tracer, appId, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: misc
operationId: getEnv
*/
func Getenv(ctx context.Context, c *client.Client, tracer trace.Tracer, appId string, opts ...Option) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "getEnv", trace.WithAttributes(attribute.String("appId", appId)))
	defer span.End()

	path := utils.Path("/v2/application/%s/environment", appId)

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
