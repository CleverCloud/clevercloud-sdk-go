// Code generated by generate-services. DO NOT EDIT.

package misc

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	trace "go.opentelemetry.io/otel/trace"
)

/*
GetPing GET /v2/ping — simple health check.

📥 **Algo Source (Legacy):**
Return "pong" as plain text, no auth, no DB access.
  - PingAPI is annotated `@Produces(MediaType.TEXT_PLAIN)` at the class level.
    Payara performs strict JAX-RS content negotiation: when the client's
    `Accept` header cannot accept `text/plain` (e.g. `Accept: application/json`
    only), Payara never reaches the handler and replies 406 Not Acceptable
    with a Payara error page (text/html), not "pong".

🔧 **Algo Rust (Implementation):**
  - Conformity: faithful — reproduce the Accept-header negotiation.
  - If the `Accept` header allows text/plain (`* /*`, `text/*`, `text/plain`,
    or no Accept header), return 200 "pong".
  - Otherwise return 406 Not Acceptable (matching Payara strict negotiation).

Source: cc-api PingAPI.java:32-51 — @Produces(MediaType.TEXT_PLAIN) + ping()
Behavior: returns "pong" as plain text, no auth required; 406 if text/plain not acceptable
Issue: #723, #1214

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability

# Returns the operation result or an error

Example:

	response := misc.GetPing(ctx, client, tracer)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: misc
operationId: get_ping
*/
func GetPing(ctx context.Context, c *client.Client, tracer trace.Tracer) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "get_ping")
	defer span.End()

	path := utils.Path("/v2/ping")

	// Make API call
	response := client.Get[client.Nothing](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
