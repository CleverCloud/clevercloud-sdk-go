// Code generated by generate-services. DO NOT EDIT.

package misc

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
GetPingStats GET /v2/ping/stats — internal runtime statistics.

📥 **Algo Source (Legacy):**
Return basic runtime stats (instance number, counters); if ?full=secret_token include top-N OAuth users, internal auth, AMQP actions; counters currently return zeros.
- Legacy note: cc-api keeps runtime counters in static fields (downscalesCount, bdsRequests, etc.); we return zeros until runtime metrics are wired.

🔧 **Algo Rust (Implementation):**
- Conformity: enhanced
Source: cc-api PingAPI.java:74-77 stats(@QueryParam("full"))
Behavior: returns basic stats; if full == secret token, includes top-N lists
Note: We don't have runtime counters yet, so all values are zeros/empty.
Issue: #723

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := misc.GetPingStats(ctx, client, tracer, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: misc
operationId: get_ping_stats
*/
func GetPingStats(ctx context.Context, c *client.Client, tracer trace.Tracer, opts ...Option) client.Response[models.PingStats] {
	ctx, span := tracer.Start(ctx, "get_ping_stats")
	defer span.End()

	path := utils.Path("/v2/ping/stats")

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[models.PingStats](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
