// Code generated by generate-services. DO NOT EDIT.

package cc_admin

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
SearchAdmin **Legacy**: ccadmin GlobalSearch controller — magic search bar
**Algorithm**:
  - ILIKE pattern %query% against users (email/name), organisations (name), applications (name/id)
  - Each entity type capped at 50 results to avoid large payloads
  - Empty query returns empty results (no error)

**Conformity**: YES — global search matches ccadmin search behavior

GET /v2/cc/search — global admin search across users, organisations, applications.

Source: ccadmin GlobalSearch controller — magic search bar
Spec: analysis-output/34-ccadmin-analysis.md §Global Search
Spec: analysis-output/livrables/LIVRABLE-B-Architecture.md §B.9
Behavior:
  - returns 401 without auth, 403 if not almighty token
  - searches users by email ILIKE or name ILIKE
  - searches organisations by name ILIKE
  - searches applications by name ILIKE or id ILIKE
  - results capped at 50 per entity type to avoid large payloads

Issue: #373

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := cc_admin.SearchAdmin(ctx, client, tracer, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: cc_admin
operationId: search_admin
*/
func SearchAdmin(ctx context.Context, c *client.Client, tracer trace.Tracer, opts ...Option) client.Response[models.AdminSearchResultView] {
	ctx, span := tracer.Start(ctx, "search_admin")
	defer span.End()

	path := utils.Path("/v2/cc/search")

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[models.AdminSearchResultView](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
