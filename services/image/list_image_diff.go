// Code generated by generate-services. DO NOT EDIT.

package image

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Listimagediff GET /v4/images/{image}/versions/{version}/diff/{newVersion}

📥 **Algo Source (Legacy):**
  - No authentication
  - Resolve both images by label, older first
  - `ImageDiff` unions the `(category, name)` keys, then the slot keys, and
    emits only the slots whose raw version differs, `-` standing for absent
  - Source: ovd ImageController.scala:120-129 + Image.scala:139-168

🔧 **Algo Rust (Implementation):**
- Two `require_by_label` calls in declaration order
- `models::compute_diff` — same union, same `-`, same ordering
- `Json(Vec<PackageDiff>)`

Source: ovd modules/image/api/routes.scala:76-85 (listImageDiff)
Behavior: public; lists the slots whose version moved between two images.
Issue: #313

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - image: Runtime name
  - version: Older image date
  - newVersion: Newer image date

# Returns the operation result or an error

Example:

	response := image.Listimagediff(ctx, client, tracer, image, version, newVersion)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: image
operationId: listImageDiff
*/
func Listimagediff(ctx context.Context, c *client.Client, tracer trace.Tracer, image string, version string, newVersion string) client.Response[[]models.PackageDiff] {
	ctx, span := tracer.Start(ctx, "listImageDiff", trace.WithAttributes(attribute.String("image", image), attribute.String("version", version), attribute.String("newVersion", newVersion)))
	defer span.End()

	path := utils.Path("/v4/images/%s/versions/%s/diff/%s", image, version, newVersion)

	// Make API call
	response := client.Get[[]models.PackageDiff](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
