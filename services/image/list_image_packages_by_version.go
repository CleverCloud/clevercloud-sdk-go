// Code generated by generate-services. DO NOT EDIT.

package image

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Listimagepackagesbyversion GET /v4/images/{image}/versions/{version} — the same, by label.

📥 **Algo Source (Legacy):**
  - No authentication
  - `resolveImageByRuntime` builds `clevercloud-runtime-<runtime>:<version>`
    and resolves it through the KV secondary index
  - Source: ovd ImageController.scala:103-108 + routes.scala:53-61

🔧 **Algo Rust (Implementation):**
  - `MultiSlotsQuery::decode` → 400
  - `ImageRepository::packages_by_label` on `images.name`, which is the same
    `<runtime>:<version>` label; the 404 still quotes the KV key clients read
  - `models::sorted_packages` → `Json(Vec<ExherboPackage>)`

Source: ovd modules/image/api/routes.scala:53-61 (listImagePackagesByVersion)
Behavior: public; the same listing, reached by label instead of identifier.
Issue: #313

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - image: Runtime name
  - version: Image date, YYYYMMDD
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := image.Listimagepackagesbyversion(ctx, client, tracer, image, version, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: image
operationId: listImagePackagesByVersion
*/
func Listimagepackagesbyversion(ctx context.Context, c *client.Client, tracer trace.Tracer, image string, version string, opts ...Option) client.Response[[]models.ExherboPackage] {
	ctx, span := tracer.Start(ctx, "listImagePackagesByVersion", trace.WithAttributes(attribute.String("image", image), attribute.String("version", version)))
	defer span.End()

	path := utils.Path("/v4/images/%s/versions/%s", image, version)

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[[]models.ExherboPackage](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
