// Code generated by generate-services. DO NOT EDIT.

package network_group

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
Getwireguardconfigpresignedurl GET .../peers/{peer_id}/wireguard/configuration/presigned-url

Algo Source: ovd NetworkGroupWireguardController.scala generatePresignedURL —
mints a Biscuit token (identity fact + 60-min expiry check + operation
check/rule) and returns a URL pointing at the config endpoint with the token
as `authorization`.

Algo Rust: re-ported against [`crate::cache::NgCache`] (peer existence check)
instead of PG. The token and URL are OVD's, on the module-scoped root key
`MODULE_PRODUCT_NETWORKGROUP__BISCUIT_ROOT_HEX` and base
`MODULE_PRODUCT_NETWORKGROUP__BASE_URL` (OVD `ccapi.url`) — see
[`crate::wireguard_presign`] (refs #3024).

Source: references/legacy/ovd/modules/networkgroup/controllers/NetworkGroupWireguardController.scala generatePresignedURL
Behavior: 200 + presigned URL; 401 another owner's group; 404 unknown NG or

	peer; 500 when the token cannot be minted (OVD `BiscuitError`).

Issue: #849, #3024

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - ownerId:
  - networkGroupId:
  - peerId:

# Returns the operation result or an error

Example:

	response := network_group.Getwireguardconfigpresignedurl(ctx, client, tracer, ownerId, networkGroupId, peerId)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: network_group
operationId: getWireguardConfigPresignedURL
*/
func Getwireguardconfigpresignedurl(ctx context.Context, c *client.Client, tracer trace.Tracer, ownerId string, networkGroupId string, peerId string) client.Response[models.PresignedURL] {
	ctx, span := tracer.Start(ctx, "getWireguardConfigPresignedURL", trace.WithAttributes(attribute.String("ownerId", ownerId), attribute.String("networkGroupId", networkGroupId), attribute.String("peerId", peerId)))
	defer span.End()

	path := utils.Path("/v4/networkgroups/organisations/%s/networkgroups/%s/peers/%s/wireguard/configuration/presigned-url", ownerId, networkGroupId, peerId)

	// Make API call
	response := client.Get[models.PresignedURL](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
