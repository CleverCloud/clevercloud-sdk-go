// Code generated by generate-services. DO NOT EDIT.

package container_registry

import (
	"context"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
RebuildContainerRegistry POST /v4/addon-providers/addon-container-registry/addons/{addon_id}/rebuild — rebuild a container registry.

Source: references/legacy/ovd/modules/container-registry/controllers/ContainerRegistryController.scala (infrastructure action)
Behavior: triggers a rebuild of the underlying container registry infrastructure.

	In practice this would call the cellar/deployment backend; here we verify the registry exists.

Issue: #643

**Legacy**: ovd ContainerRegistryController.scala (infrastructure rebuild action)
**Algorithm**:
  - Verify registry exists, trigger backend rebuild (stub), return 204

**Conformity**: STUB (backend infra call not wired)

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - addon_id: Container registry addon ID

# Returns the operation result or an error

Example:

	response := container_registry.RebuildContainerRegistry(ctx, client, tracer, addon_id)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: container_registry
operationId: rebuild_container_registry
*/
func RebuildContainerRegistry(ctx context.Context, c *client.Client, tracer trace.Tracer, addon_id string) client.Response[client.Nothing] {
	ctx, span := tracer.Start(ctx, "rebuild_container_registry", trace.WithAttributes(attribute.String("addon_id", addon_id)))
	defer span.End()

	path := utils.Path("/v4/addon-providers/addon-container-registry/addons/%s/rebuild", addon_id)

	// Make API call
	response := client.Post[client.Nothing](ctx, c, path, nil)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
