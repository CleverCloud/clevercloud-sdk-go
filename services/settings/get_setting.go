// Code generated by generate-services. DO NOT EDIT.

package settings

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	attribute "go.opentelemetry.io/otel/attribute"
	trace "go.opentelemetry.io/otel/trace"
)

/*
GetSetting GET /v4/console/settings/{name} — get a single setting by name.

📥 **Algo Source (Legacy):**
Retrieve a single console setting by name for the authenticated user.
- OVD SettingsActor.scala receives GetByKey(name) action
- Builds Redis hash key "{user_id}_{env}"
- Calls SettingsRepository.get() which does HGET on the hash field
- Returns Setting(name, value) or 404 SettingsKeyNotFound
- Source: OVD SettingsActor.scala GetByKey, SettingsRepository.scala HGET

🔧 **Algo Rust (Implementation):**
- Extract user_id from OAuth1 auth token
- Build Redis hash key "{user_id}_{env}"
- HGET via cache module for the specific field name
- Return 200 with SettingView if found, 404 if absent

Source: OVD SettingsActor.scala — GetByKey action
Source: OVD SettingsRepository.scala — HGET
Spec: analysis-output/03-ovd-analysis.md §Settings
Behavior: 401 if no valid auth, 404 if setting not found, 200 with SettingView on success
Issue: #376

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - name: Setting name
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := settings.GetSetting(ctx, client, tracer, name, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: settings
operationId: get_setting
*/
func GetSetting(ctx context.Context, c *client.Client, tracer trace.Tracer, name string, opts ...Option) client.Response[models.SettingView] {
	ctx, span := tracer.Start(ctx, "get_setting", trace.WithAttributes(attribute.String("name", name)))
	defer span.End()

	path := utils.Path("/v4/console/settings/%s", name)

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[models.SettingView](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
