// Code generated by generate-services. DO NOT EDIT.

package settings

import (
	"context"
	"fmt"
	client "go.clever-cloud.dev/client"
	utils "go.clever-cloud.dev/sdk/internal/utils"
	models "go.clever-cloud.dev/sdk/models"
	trace "go.opentelemetry.io/otel/trace"
)

/*
ListSettings GET /v4/console/settings — list all settings for the authenticated user and environment.

📥 **Algo Source (Legacy):**
List all console settings for the authenticated user in a given environment.
- OVD SettingsActor.scala receives GetAll action
- Builds Redis hash key "{user_id}_{env}" (env defaults to "prod")
- Calls SettingsRepository.getAll() which does HGETALL on the hash
- Returns all key-value pairs as List[Setting(name, value)]
- Source: OVD SettingsActor.scala GetAll, SettingsRepository.scala HGETALL

🔧 **Algo Rust (Implementation):**
- Extract user_id from OAuth1 auth token
- Build Redis hash key "{user_id}_{env}" using query param (default "prod")
- HGETALL via cache module, map to Vec<SettingView>
- Return 200 with JSON array (may be empty)

Source: OVD SettingsActor.scala — GetAll action
Source: OVD SettingsRepository.scala — HGETALL
Spec: analysis-output/03-ovd-analysis.md §Settings
Behavior: 401 if no valid auth, 200 with list of SettingView (may be empty)
Issue: #376

Parameters:
  - ctx: context for the request
  - client: the Clever Cloud client
  - tracer: OpenTelemetry tracer for observability
  - opts: optional query parameters

# Returns the operation result or an error

Example:

	response := settings.ListSettings(ctx, client, tracer, opts...)
	if response.HasError() {
		// Handle error
	}
	result := response.Payload()

x-service: settings
operationId: list_settings
*/
func ListSettings(ctx context.Context, c *client.Client, tracer trace.Tracer, opts ...Option) client.Response[[]models.SettingView] {
	ctx, span := tracer.Start(ctx, "list_settings")
	defer span.End()

	path := utils.Path("/v4/console/settings")

	// Build query parameters
	query := buildQueryString(opts...)
	if query != "" {
		path = fmt.Sprintf("%s?%s", path, query)
	}

	// Make API call
	response := client.Get[[]models.SettingView](ctx, c, path)

	if response.HasError() {
		span.RecordError(response.Error())
	}

	return response
}
