// Code generated by generate-services. DO NOT EDIT.

package settings

import (
	"fmt"
	"net/url"
	"strings"
)

// Option defines a functional option for settings operations
type Option func(*Options)

// Options holds query parameters for settings operations
type Options struct {
	Env *string `url:"env,omitempty"`
}

// WithEnv sets the env query parameter
func WithEnv(env string) Option {
	return func(o *Options) {
		o.Env = &env
	}
}

// buildQueryString builds a query string from options
func buildQueryString(opts ...Option) string {
	options := &Options{}
	for _, opt := range opts {
		opt(options)
	}

	var params []string
	if options.Env != nil {
		params = append(params, fmt.Sprintf("env=%s", url.QueryEscape(*options.Env)))
	}

	if len(params) == 0 {
		return ""
	}
	return strings.Join(params, "&")
}
